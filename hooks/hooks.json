{
  "description": "Protect immutable files during deep-evolve experiment runs (v3.0+: TOOL_NAME guard, initializing-status bypass, optional seal_prepare_read Read-branch, META_MODE-gated writes for prepare/program/strategy)",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit|Bash|Read",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/protect-readonly.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
